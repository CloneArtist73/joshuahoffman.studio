# InMotion FTP Upload Plugin

This repo-local Codex plugin packages an upload workflow for `joshuahoffman.studio` and
other InMotion hosting targets.

Primary entry point:

- `scripts/upload-to-inmotion.ps1`

If you already use CI/CD, you can copy the same command into a deployment step.
